# Used by sqlx-cli when regenerating the .sqlx/ query cache via
# `cargo sqlx prepare`. Not read at build/test time — those use the
# checked-in .sqlx/ cache via sqlx's offline mode.
DATABASE_URL=sqlite://./dev.db
